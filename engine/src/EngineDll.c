// dllmain.cpp : Defines the entry point for the DLL application.

// TODO: manually resolve all these windows api calls
/*
#define WIN32_LEAN_AND_MEAN
#include <windows.h>

// TODO:
// EngineDll (selfloading through persistence methods and stuff?)

// BeaconDll (unload when not in use (clear memory only))
// CommandDll (unload when not in use (clear memory only))
// UninstallDll

BOOL WINAPI DllMain(HMODULE hModule, DWORD ul_reason_for_call, LPVOID lpReserved)
{
    switch (ul_reason_for_call)
    {
    case DLL_PROCESS_ATTACH:
        MessageBoxA(NULL, "Engine.axe/Payload.axe", "Debug", MB_OK);
        break;
    case DLL_THREAD_ATTACH:
    case DLL_THREAD_DETACH:
    case DLL_PROCESS_DETACH:
        break;
    }
    return TRUE;
}
*/
